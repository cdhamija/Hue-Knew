{
  "name": "hue-knew",
  "version": "1.0.0",
  "private": true,
  "description": "Tap to discover rare and wonderful colors, one by one.",
  "scripts": {
    "serve": "node serve.js",
    "sync": "cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.0",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0"
  }
}
